<h2> {{model.author}} </h2>

<h3> {{model.content}} </h3>

{{#each model.answers as |answer|}}
  <li> {{answer.author}} </li>
  <li> {{answer.content}} </li>
{{/each}}

{{new-answer saveAnswer="saveAnswer" question=model}}
