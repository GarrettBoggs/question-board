import { questionEmpty } from 'question-board/helpers/question-empty';
import { module, test } from 'qunit';

module('Unit | Helper | question empty');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionEmpty([42]);
  assert.ok(result);
});
